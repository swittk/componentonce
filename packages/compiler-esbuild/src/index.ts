/** Placeholder compiler surface. Compiler lane owns the concrete trusted-bundle contract. */
export interface ComponentOnceCompileInput {
  readonly source: string;
  readonly sourceFileName?: string;
}
